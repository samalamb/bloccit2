$('.js-points').html("<%= @post.points %>")
$('.js-total').html("<div><%= pluralize(@post.points, 'point') %></div>")
$('.js-votes').html("<%= pluralize(@post.up_votes, 'up vote') %>, <%= pluralize(@post.down_votes, 'down vote') %>")
